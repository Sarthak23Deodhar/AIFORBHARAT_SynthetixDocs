# VITE Environment Variables for Cognito Auth
# ─────────────────────────────────────────────────────────
# After deploying the backend with `serverless deploy`:
#   cd serverless-backend
#   serverless info --verbose
# Copy the Stack Output values for UserPoolId and UserPoolClientId below.

VITE_COGNITO_USER_POOL_ID=us-east-1_XXXXXXXXX
VITE_COGNITO_CLIENT_ID=XXXXXXXXXXXXXXXXXXXXXXXXXX
