const fetch = require('node-fetch'); async function test() { try { const res = await fetch('https://ihm3s4vc87.execute-api.us-east-1.amazonaws.com/dev/api/generate-wiki', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ code_snippet: 'int main() { return 0; }', language: 'en', skill_level: 'junior' }) }); const text = await res.text(); console.log(text); } catch(e) {} } test();  
